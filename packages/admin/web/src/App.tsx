export function App() {
  return (
    <main
      style={{
        fontFamily: "system-ui, sans-serif",
        padding: "2rem",
        maxWidth: "48rem",
      }}
    >
      <h1>SlugBase Admin</h1>
      <p>Cloud operator portal — dashboards and directories ship in later tasks.</p>
    </main>
  );
}

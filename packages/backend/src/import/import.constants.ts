/** Max bookmarks per import request (spec §13, def §2). */
export const MAX_IMPORT_BOOKMARKS = 5_000;

/** Max Netscape HTML upload size in bytes (spec §13, def §2). */
export const MAX_NETSCAPE_HTML_BYTES = 5 * 1024 * 1024;

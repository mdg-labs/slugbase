import { defaultDomainModules } from "./domain-modules.registry.js";

export {
  BILLING_MODULE,
  createDomainModules,
  defaultDomainModules,
} from "./domain-modules.registry.js";

export const domainModules = defaultDomainModules;

name: Deploy to Fly.io (Staging)

on:
  push:
    branches:
      - dev
      - staging
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to slugbase-staging
    runs-on: ubuntu-latest
    concurrency: deploy-staging

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io
        run: flyctl deploy --remote-only -a slugbase-staging --build-arg COMMIT_SHA=${{ github.sha }} --build-arg VITE_SENTRY_DSN=${{ secrets.SENTRY_DSN_STAGING }} --build-arg VITE_SENTRY_DEBUG=true --build-arg VITE_FEATUREBASE_APP_ID=${{ secrets.FEATUREBASE_APP_ID }}
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
          FLY_ORG: ${{ secrets.FLY_ORG }}

# Deprecated — use Linear board reference

**This file is deprecated.** SlugBase agent workflow now uses **Linear** (SlugBase team) as the primary tracker.

→ **[linear-board.md](linear-board.md)** — tool matrix, workflow states, LINEAR SYNC blocks, commit linking.

GitHub Issues remain as a **two-way synced mirror** of Linear (`mdg-labs/slugbase`). Agents must not set GitHub Projects v2 board Status.
